const en_US = {
  monitoring: {
    hint: 'Container and Kubernetes metrics come from the selected metric datasource. Collapsed sections issue no queries.',
    datasource: 'Metric datasource',
    datasource_hint: 'The embedded store only holds n9e self metrics; cluster metrics live in a long-term store such as Thanos.',
    scope: 'Cluster / namespace',
    no_datasource: 'No metric datasource available',
    no_scope:
      'No container metrics found for "{{service}}". This tab matches on the "container name == service name" convention and needs a cluster label; you can also try another metric datasource.',
    load_failed: 'Failed to load monitoring metrics',
    chart_empty: 'No time series in this range',
    legend_more: '{{count}} more',
    section: {
      summary: 'Summary',
      traffic: 'Traffic & errors',
      workload: 'Workload resources',
      replicas: 'Replicas & pods',
      jvm: 'JVM',
      node: 'Nodes',
      middleware: 'Middleware',
    },
    stat: {
      traffic: 'Traffic',
      traffic_hint: 'From spanmetrics. Shows "not instrumented" when RED is missing — never a fake 0. P95 is milliseconds.',
      ready: 'Ready replicas',
      ready_hint: 'Ready / desired. Restarts are the count inside the selected window.',
      resource: 'Resource headroom',
      resource_hint: 'Usage / limit. Missing limits render as — rather than 0%.',
      qps: 'QPS',
      error_rate: 'Error rate',
      p95: 'P95',
      ready_replicas: 'Ready',
      restarts: 'Restarts',
      oom: 'OOMKilled',
      cpu_water: 'CPU',
      mem_water: 'Memory',
      uninstrumented: 'Not instrumented',
    },
    panel: {
      cpu: 'CPU usage vs request / limit (cores)',
      cpu_hint: 'Solid lines are per-pod usage, dashed lines are request / limit. Usage riding the limit means the service needs more CPU.',
      cpu_throttling: 'CPU throttling ratio',
      cpu_throttling_hint: 'Share of scheduling periods that got throttled. Above 5% the CPU limit is too low — the only signal that directly proves "not enough CPU".',
      memory: 'Memory working set vs request / limit',
      memory_hint: 'Working set is what the OOM killer looks at: reaching the limit means OOMKilled.',
      network: 'Network throughput',
      network_hint: 'Network metrics carry no container label, so the pod set is intersected instead of regex-matching pod names (which collides with same-prefix services).',
      qps: 'QPS',
      error_rate: 'Error rate',
      p95_ms: 'P95 (ms)',
      p95_ms_hint: 'The spanmetrics histogram is milliseconds. HTTP route latency below is seconds; both units are shown as-is.',
      http_status: 'HTTP status QPS',
      http_status_hint: 'From http_server_request_duration_seconds (seconds). Empty means HTTP metrics are not present, not a failure.',
      http_route: 'Top routes QPS',
      http_route_hint: 'Aggregated by http_route. Empty when that label is missing.',
      http_slow: 'Slow routes P95 (s)',
      http_slow_hint: 'The HTTP histogram is seconds, unlike spanmetrics milliseconds above.',
      client: 'Downstream client QPS',
      client_hint: 'spanmetrics calls with span_kind=CLIENT — currently the only real dependency view. Empty when that dimension is absent.',
      replicas: 'Replica trend',
      replicas_hint: 'Matched by deployment name. Empty for StatefulSets that have no kube_deployment_* series.',
      restarts: 'Container restarts',
      restarts_hint: 'increase() over each step window. Spikes are restarts.',
      waiting: 'Waiting reasons',
      waiting_hint: 'Containers currently Waiting. An empty table means nothing is stuck.',
      pods: 'Pod inventory',
      pods_hint: 'The JVM column joins via the pod segment of exported_instance; unmatched or uninstrumented cells are —.',
      jvm_heap: 'Heap used vs limit',
      jvm_heap_hint: 'OTel runtime-telemetry names, not Micrometer. Legend uses the pod segment of exported_instance.',
      jvm_pool: 'Memory pool used',
      jvm_gc: 'GC time ratio',
      jvm_gc_hint: 'rate(jvm_gc_duration_seconds_sum), roughly wall-clock share.',
      jvm_after_gc: 'Heap after last GC',
      jvm_after_gc_hint: 'jvm_memory_used_after_last_gc_bytes. Empty when that metric is not collected.',
      jvm_threads: 'Threads',
      jvm_classes: 'Loaded classes',
      jvm_cpu: 'JVM CPU',
      jvm_cpu_hint: 'jvm_cpu_recent_utilization(_ratio).',
      node_cpu: 'Node CPU usage',
      node_memory: 'Node memory usage',
      node_disk: 'Node disk usage',
      node_hint: 'Joins kube_pod_info.node to node-exporter instance (port stripped). No PSI.',
    },
    legend: {
      request: 'request',
      limit: 'limit',
      receive: 'rx',
      transmit: 'tx',
      available: 'available',
      desired: 'desired',
      unavailable: 'unavailable',
    },
    table: {
      pod: 'Pod',
      node: 'Node',
      ip: 'Pod IP',
      qos: 'QoS',
      cpu: 'CPU',
      cpu_water: 'CPU headroom',
      memory: 'Memory',
      mem_water: 'Memory headroom',
      jvm: 'JVM heap',
      reason: 'Reason',
      waiting_empty: 'No containers are Waiting',
      pods_empty: 'No pods to list',
    },
    jvm: {
      empty:
        'This service has no JVM runtime metrics yet. They appear after the OTel Java agent runtime-telemetry is enabled; an empty panel is not an outage. The current pilot is mostly rome-sec.',
    },
    middleware: {
      empty: 'This release cannot derive which middleware instances a service uses from labels, so this section stays empty. No cluster-wide middleware overview link is shown.',
    },
  },
};

export default en_US;
