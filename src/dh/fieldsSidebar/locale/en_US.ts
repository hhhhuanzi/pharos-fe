const en_US = {
  popular_fields: 'Popular Fields',
  popular_fields_tip: 'Fields you often add to "Show Fields", plus common log fields such as time, message, level, trace and container',
  empty_fields: 'Empty Fields',
  empty_fields_tip: 'Fields defined in the index mapping but with no value in the current query result. Collapsed by default, auto-expanded while searching',
  available_fields_no_result_tip: 'No query result sample yet, so this shows every field in the index mapping. Run a query to split out "Empty Fields" based on the result',
  popular_fields_no_result_tip: 'No query result sample yet, so these are candidate fields matched against the built-in recommendation list, not confirmed to have values in this index. Run a query to narrow this down to fields that actually have values',
};

export default en_US;
